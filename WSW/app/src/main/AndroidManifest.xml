<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wsw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WSW">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">

        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".feature.signup.SignupActivity" />
        <activity android:name=".feature.login.LoginActivity" />

        <activity android:name=".RandomMessageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RandomMessageViewActivity"/>



        <activity android:name=".ui.studyboard.StudyBoardPostActivity" />
        <activity android:name=".ui.studyboard.StudyBoardWriteActivity" />

        <activity android:name=".ui.reviewboard.ReviewBoardPostActivity" />
        <activity android:name=".ui.reviewboard.ReviewBoardWriteActivity" />

        <activity android:name=".ui.secretboard.post.SecretBoardPostActivity" />
        <activity android:name=".ui.secretboard.post.SecretBoardWriteActivity" />

        <activity android:name=".ui.questionboard.QuestionBoardPostActivity" />
        <activity android:name=".ui.questionboard.QuestionBoardWriteActivity" />

    </application>

</manifest>